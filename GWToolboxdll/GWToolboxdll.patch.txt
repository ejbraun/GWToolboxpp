## v1 - 2026-09-05
- Add GWRL launcher communication for user-requested coordinated updates.
- Display the fork revision separately from the upstream Toolbox version.
- Support crash dumps with plugins loaded and retain plugin diagnostics.
- Fix DBBox drawing and cooperative plugin unloading.

## v2 - 2026-09-05
- Fix shared Main Window button IDs for plugins and DBBox features.
- Stabilize GWRL update notifications and report launcher acknowledgement, rejection, disconnects and timeouts.
- Add a default-disabled switch for Game Settings name tag recoloring.
- Publish only the integer fork revision in the Toolbox manifest.
- Fix the game-exit assertion caused by finalizing plugins before asynchronous unload work completed.

## v3 - 2026-09-08
- Apply the hidden-NPC targeting bypass consistently to minimap, hotkey and chat targeting while retaining category and corpse filters.
- Keep decoded names free of markup after changing the game text language.
- Fix collapsed Objective Timer runs disappearing and restore the visibility of saved run history.
- Save Objective Timer snapshots in the background, merge history across sessions and clients, and safely finish pending writes during shutdown.

## v4 - 2026-09-08
- Fix the Add Teambuild crash caused by delayed skill-name decoding writing to a deleted build, while preserving automatic profession and elite-skill labels.
- Preserve normal camera sensitivity with the cursor fix enabled and reject stale cursor-warp movement.
- Validate raw mouse input, correct cursor mask caching, and clean up cursor hooks and pending redraws when unloading Mouse Settings.
- Allow chat commands and hotkeys to run while Guild Wars is in the background, and recover hotkey readiness after early injection, settings reloads, and map changes.
- Refresh GWCA's key-input frame and defer key release to the next game loop so skill requests survive UI recreation and queued input processing.

## v5 - 2026-09-11
- Publish the latest teambuild and cursor fixes under a new launcher update revision.
- Include hotkey startup recovery, background hotkey and chat-command support, and the skill-input compatibility fixes.
- Preserve agent-existence validation in the hidden-NPC targeting bypass, preventing Guild Wars assertions when a saved or queued target has despawned.
- Preserve the active keyboard layout during startup layout detection, reuse an already installed en-US layout, and validate the native layout address before accessing it.
